// Select which editorially reviewed projects appear on the homepage.
// GitHub can enrich these records, but it cannot replace role and case-study context.
export const showcaseProjectSlugs = ["dmit-frontend-web"] as const;
